mixin loginForm()
    form.form(action="/loing" method="Post")
        h2 Login
        label(for="email") Email Address
        input(type="email" name="email")
        label(for="password") Password
        input(type="password" name="password")
        input(type="submit" value="Submit")

